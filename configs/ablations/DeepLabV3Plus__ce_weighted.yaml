# Ablation cell: arch=DeepLabV3Plus, loss=ce_weighted.
# Generated by scripts/run_ablations.py -- edit the matrix there, not here.
# Fixed for fairness: seed=42, epochs=40. Headline metrics: oil IoU & oil recall.
model:
  arch: DeepLabV3Plus
  encoder: resnet50
  encoder_weights: imagenet
  in_channels: 3
  num_classes: 5
data:
  root: data/datasets/oil_spill
  image_size: 256
  batch_size: 8
  num_workers: 4
  val_split: 0.2
  split_seed: 42
optim:
  lr: 0.0003
  weight_decay: 0.0001
  epochs: 40
  optimizer: adamw
  scheduler: cosine
loss:
  type: ce
  class_weights: auto
runtime:
  seed: 42
  amp: true
  device: auto
  precision: bf16
  deterministic: true
early_stopping:
  enabled: true
  monitor: val_oil_iou
  mode: max
  patience: 10
  min_delta: 0.0
checkpoint:
  dir: artifacts/checkpoints
  save_best: true
  save_last: true
  monitor: val_oil_iou
  mode: max
mlflow:
  experiment_name: oil-spill-segmentation
  tracking_uri: ./mlruns
  log_samples: 4
  run_name: abl-deeplabv3plus-ce_weighted
